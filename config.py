"""
SporraMom W3 智能客服系统 — 配置管理
集中管理所有可调参数，便于评测时做 A/B 对比。
"""

import os

# ============================================================
# LLM API 配置
# ============================================================
API_BASE_URL = os.getenv("API_BASE_URL", "https://your-api-base-url.com/v1")
API_KEY = os.getenv("API_KEY", "your-api-key-here")
LLM_MODEL = os.getenv("LLM_MODEL", "your-model-name")

# ============================================================
# Embedding 模型配置
# ============================================================
EMBEDDING_MODEL = "BAAI/bge-small-zh-v1.5"  # ~90MB, 中文优化, GPU 友好
EMBEDDING_DIM = 512  # bge-small-zh 输出维度

# ============================================================
# Reranker 配置
# ============================================================
RERANKER_MODEL = "BAAI/bge-reranker-base"

# ============================================================
# 检索参数 (A/B 实验核心变量)
# ============================================================
BM25_TOP_K = 8            # BM25 初筛候选数
VECTOR_TOP_K = 8          # 向量检索初筛候选数
RRF_K = 60                # RRF 融合参数 (论文推荐 60)
RERANK_ENABLED = True     # 是否启用 Reranker (True=质量优先 92%HR@3/19s, False=速度优先 76%HR@3/11s)
RERANK_TOP_N = 5          # Rerank 后保留的最终候选数
RELEVANCE_THRESHOLD = 0.05 # Rerank 相关性阈值, 低于此触发降级策略

# ============================================================
# 分块参数
# ============================================================
CHUNK_MAX_CHARS = 800     # 单 chunk 最大字符数
CHUNK_OVERLAP = 100       # chunk 重叠字符数 (仅对超长段落生效)

# ============================================================
# LLM 生成参数
# ============================================================
LLM_TEMPERATURE = 0.3     # 低温度 → 更忠实于原文
LLM_MAX_TOKENS = 1024
LLM_TIMEOUT = 30          # 秒

# ============================================================
# 自检参数
# ============================================================
SELF_CHECK_MIN_COVERAGE = 0.3  # 答案关键词覆盖率最低阈值
SELF_CHECK_MAX_RETRIES = 1     # 自检失败后最多重试次数

# ============================================================
# 评测参数
# ============================================================
EVAL_TOP_K_VALUES = [1, 3, 5, 10]  # 评测时计算多个 K 的 Hit Rate

# ============================================================
# 文件路径
# ============================================================
PRODUCT_MANUAL_PATH = os.path.join(os.path.dirname(__file__), "产品说明书.md")
INDEX_DIR = os.path.join(os.path.dirname(__file__), "index_cache")
TEST_SET_PATH = os.path.join(os.path.dirname(__file__), "test_set.json")
